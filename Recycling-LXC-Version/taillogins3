#!/bin/bash

process_new_line() {
    # local var=''
    read
    while true
    do

        echo $REPLY | cut -d ";" -f 2 | sudo tee /LoginIPs3
        # Need to add iptables rule

        read
    done
}

touch /LoginIPs3
tail -f /MITM/logs/logins/container3.log | process_new_line